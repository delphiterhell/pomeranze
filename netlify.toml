[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "20"

# Abilita Netlify Identity + Git Gateway per Decap CMS
[context.production]
  command = "npm run build"
